This module provides granular visibility control over PDF and QWeb Reports per model based on User Groups:

1. It introduces the ability to configure "Allowed Reports" for specific models within Odoo Security Groups.
2. It enforces a "Deny by Default" policy for any restricted model, ensuring that users can only view and print reports explicitly permitted by their assigned groups.

This module extends Odoo's role-based security system to provide granular, role-based visibility control over PDF and QWeb Reports:

1. It introduces the ability to configure "Allowed Reports" on Access Rights.
2. It strictly enforces a "Deny by Default" policy, ensuring that users can only print the specific reports explicitly permitted by their assigned Roles.
3. Automatically populates existing report access during installation to prevent disruption.
